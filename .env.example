# 企业微信群机器人 Webhook 完整地址（在企业微信群里添加机器人后复制）
WECHAT_WORK_WEBHOOK=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxxxxxxx

# 可选：日报 JSON 路径（默认 ./data/nanjing-daily.json）
# DAILY_REPORT_PATH=/path/to/report.json

# 可选：直接传入整段 JSON（优先级高于文件；适合 CI 上一步生成后注入）
# DAILY_REPORT_JSON={"date":"2026-05-11","secondHand":[],"newHouse":[]}

# 可选：抓取南京网上房地产页面超时（毫秒），默认 25000
# FETCH_TIMEOUT_MS=25000

# 可选：关注区域（逗号分隔）；默认仅玄武区,秦淮区,建邺区,鼓楼区,雨花台区
# FOCUS_DISTRICTS=玄武区,秦淮区,建邺区,鼓楼区,雨花台区

# 可选：贝壳本地采集（仅本地；云端易触发验证码）
# 推荐 Playwright：npm run playwright:install，再 npm run beike:login
# 持久 Chromium 目录（默认 ./data/beike-chromium-profile/，勿提交 git）
# BEIKE_PROFILE_DIR=/绝对路径/data/beike-chromium-profile
# storageState 备份路径（默认 ./data/beike-storage.json）
# BEIKE_STORAGE_PATH=/绝对路径/data/beike-storage.json
# 连接你已手动打开的 Chrome（极验封禁时可试）：先启动
#   /Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --remote-debugging-port=9222 --user-data-dir=/tmp/beike-chrome-manual
# 再设 BEIKE_CDP_URL=http://127.0.0.1:9222 后执行 npm run beike:login
# BEIKE_CDP_URL=http://127.0.0.1:9222
# 抓取可单独指定 CDP（不设则沿用 BEIKE_CDP_URL）；仍弹极验时务必用此方式连接「已手动过极验」的同一 Chrome
# BEIKE_FETCH_CDP_URL=http://127.0.0.1:9222
# BEIKE_PAUSE_MS=5000
# BEIKE_LIST_WAIT_MS=20000
# 遇极验时在终端按回车再继续（你在浏览器里先完成验证）
# BEIKE_INTERACTIVE=1
# BEIKE_CAPTCHA_RETRY=6
# 只抓一个区，减少连续跳转触发极验（CDP 下建议先开）
# BEIKE_ONLY_FIRST_DISTRICT=1
# 自定义区：slug 或完整 URL，逗号分隔，例如 xuanwu,qinhuai
# BEIKE_DISTRICT_URLS=xuanwu,qinhuai
# BEIKE_LOGIN_START_URL=https://nj.ke.com/chengjiao/xuanwu/
# 抓取时无头浏览器：默认开启；调试可设为 0 看页面
# BEIKE_HEADLESS=0
# 登录脚本是否用本机 Chrome 渠道（Mac 推荐默认；无 Chrome 时设 BEIKE_USE_CHROME=0）
# BEIKE_USE_CHROME=0
# 旧版裸 HTTP（易验证码），仅备用：npm run daily:fetch:ershou:http
# BEIKE_COOKIE=xxx=...;yyy=...
# BEIKE_REFERER=https://nj.ke.com/
# BEIKE_TIMEOUT_MS=15000
# BEIKE_MAX_ITEMS=30
# 列表页不展示挂牌价时，默认用同源 fetch 打开每条成交详情补全挂牌价（约每条多一次请求，可调慢延迟）；设为 0 关闭
# BEIKE_DETAIL_LIST_PRICE=0
# BEIKE_DETAIL_LIST_PRICE_DELAY_MS=350
# 已含「挂牌价+带看+关注+浏览」时跳过再次 fetch 详情（默认跳过；设 0 表示每次都拉详情刷新浏览量等）
# BEIKE_DETAIL_SKIP_IF_COMPLETE=0
